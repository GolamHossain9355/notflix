import React, {useContext, useEffect, useState} from 'react'
import {auth} from "../firebase"

const AuthContext = Reac

export default function AuthContext() {
  return (
    <div>AuthContext</div>
  )
}
